{
  "buildCommand": "turbo run build --filter=@nimble/web",
  "installCommand": "npm install",
  "framework": null,
  "outputDirectory": "apps/web/out",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://api.nimble.bitnook.cc/:path*"
    }
  ]
}