{
  "buildCommand": "turbo run build --filter=@nimble/web",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "rewrites": [
    {
      "source": "/v0/api/:path*",
      "destination": "https://nimble-api.vercel.app/:path*"
    }
  ]
}