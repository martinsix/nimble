# Pusher Configuration (Client-side)
# Get these from your Pusher app dashboard at https://pusher.com
# Only PUSHER_KEY and PUSHER_CLUSTER are needed for the client
NEXT_PUBLIC_PUSHER_KEY=your_pusher_key
NEXT_PUBLIC_PUSHER_CLUSTER=your_pusher_cluster

# API Configuration
# API_URL=http://localhost:3001  # For local development
# API_URL=https://your-api-domain.com  # For production